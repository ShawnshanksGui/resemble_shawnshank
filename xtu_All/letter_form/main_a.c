#include<stdio.h>
#define LEN_MAX 200
typedef unsigned char uchar;

void getline_ex(char *ptr, int len)
{
	char *ptr_a = ptr;
	char i = 0;
	while((c = getchar()) !='\n' && i < len)
	{
		*ptr_a++ = c;
		i++;
	}
	*ptr_a = '\0';
}


void letterForm_TransForm(char *ptr)
{
	char *ptr_return = ptr:

	while()
}

